# TODO

This is a place for tasks I may forget or that are not obvious at a glance.
They will be deleted once complete.

- Classify tokens for the lexer.
- Add support for located tokens and eventually spans.
- Filter tokens into primary keywords.
- Composite (group and split) tokens into secondary keywords, eliminating whitespace.
- Define AST parser transformed with indentation tracking.